export declare type UpdateCallMetadata_sample2 = {
    /**
     * Optional. This parameter is used to define the inline EML document. No default value is defined. If inline EML is specified, you should not include Url or ApplicationID parameter in the same REST API request, otherwise an error will be returned
     */
    Eml?: string;
};
