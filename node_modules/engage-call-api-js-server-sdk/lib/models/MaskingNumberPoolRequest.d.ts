/**
 * 1. If appType is EXTERNAL then flowName is Mandatory field.
 *
 */
export declare type MaskingNumberPoolRequest = {
    PhoneNumbers?: Array<string>;
    AppType?: MaskingNumberPoolRequest.AppType;
    UsePin?: boolean;
    /**
     * * This field contains flowId for VDT flow or http url or None ( empty ) .
 * * Mandatory if appType is EXTERNAL
 *
     */
    FlowName?: string;
    /**
     * Supported values are GET and POST.
 *
     */
    FlowHttpMethod?: string;
    PoolName?: string;
};
export declare namespace MaskingNumberPoolRequest {
    enum AppType {
        EXTERNAL = "EXTERNAL",
        INTERNAL = "INTERNAL"
    }
}
