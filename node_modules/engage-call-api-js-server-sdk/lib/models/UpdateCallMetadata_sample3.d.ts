export declare type UpdateCallMetadata_sample3 = {
    /**
     * Optional. This parameter specifies the URL of the EML document hosted in external HTTP server. No default value is defined. If this URL is provided, Radisys EDP (CPaaS) will use this URL to fetch EML document. The EML document specifies the service control flow and media function that should be enabled after call is connected. If this URL is specified, you should not include Eml or ApplicationID parameter in the same REST API request, otherwise an error will be returned.
     */
    Url?: string;
    /**
     * Optional. This parameter specifies which HTTP method should be used to request the URL to fetch the external EML document (if defined in Url parameter) once the call connects. Valid parameters are GET and POST. Default value is POST.
     */
    Method?: UpdateCallMetadata_sample3.Method;
};
export declare namespace UpdateCallMetadata_sample3 {
    /**
     * Optional. This parameter specifies which HTTP method should be used to request the URL to fetch the external EML document (if defined in Url parameter) once the call connects. Valid parameters are GET and POST. Default value is POST.
     */
    enum Method {
        POST = "POST",
        GET = "GET"
    }
}
