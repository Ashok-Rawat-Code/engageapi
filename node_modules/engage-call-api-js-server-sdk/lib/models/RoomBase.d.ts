export declare type RoomBase = {
    /**
     * Optional. A word or set of words by which a room is addressed, or referred to. Case sensitive. Only alpha-numeric characters, space and hyphen allowed as Room name. Example: 'myroom123' or 'my-room' or 'my room'
     */
    RoomName: string;
    /**
     * Optional. This parameter specifies the room type in one of the following values: Audio, Video-VAS, Video-CPV-6-split, Video-CPV-4-split, Video-CPV-ASL, Video-CPV-DHL, Video-CPV-auto-split. Default value should be Audio, setting this field as Video will enable the video in room calls.
     */
    RoomType: RoomBase.RoomType;
    /**
     * Optional. Default: None. This is tenant owned Ph used as caller ID when inviting participant to join the Room. It can be same as DialInPhone also.
     */
    CallerId?: string;
    /**
     * Optional. Default: None. This is tenant owned Ph to be used by PSTN / Mobile dial-in users.
     */
    DialInPhone?: string;
    /**
     * Optional. Default: No. Set to Yes if PSTN Dial-In required
     */
    GenerateDialInCode?: RoomBase.GenerateDialInCode;
    /**
     * Optional. This parameter specifies which call status event(s) should be sent back to the customer application via the StatusCallback URL (if specified). Supported call status events are: start, end, join, leave. A comma separated list of events can be specified here. Default value is None.
     */
    StatusCallbackEvent?: string;
    /**
     * Optional. This parameter specifies which HTTP method should be used to request the StatsCallback URL (if specified) when sending the call status event. Valid parameters are GET and POST. Default value is POST.
     */
    Method?: RoomBase.Method;
    /**
     * Optional. Default: None. Supports relative or absolute URL
     */
    StatusCallbackUrl?: string;
    /**
     * Optional. Default: 6 hrs. It takes value in terms of hours
     */
    MaxActiveRoomDuration?: number;
};
export declare namespace RoomBase {
    /**
     * Optional. This parameter specifies the room type in one of the following values: Audio, Video-VAS, Video-CPV-6-split, Video-CPV-4-split, Video-CPV-ASL, Video-CPV-DHL, Video-CPV-auto-split. Default value should be Audio, setting this field as Video will enable the video in room calls.
     */
    enum RoomType {
        AUDIO = "Audio",
        VIDEO_VAS = "Video-VAS",
        VIDEO_CPV_6_SPLIT = "Video-CPV-6-split",
        VIDEO_CPV_4_SPLIT = "Video-CPV-4-split",
        VIDEO_CPV_ASL = "Video-CPV-ASL",
        VIDEO_CPV_DHL = "Video-CPV-DHL",
        VIDEO_CPV_AUTO_SPLIT = "Video-CPV-auto-split"
    }
    /**
     * Optional. Default: No. Set to Yes if PSTN Dial-In required
     */
    enum GenerateDialInCode {
        YES = "Yes",
        NO = "No"
    }
    /**
     * Optional. This parameter specifies which HTTP method should be used to request the StatsCallback URL (if specified) when sending the call status event. Valid parameters are GET and POST. Default value is POST.
     */
    enum Method {
        GET = "GET",
        POST = "POST"
    }
}
