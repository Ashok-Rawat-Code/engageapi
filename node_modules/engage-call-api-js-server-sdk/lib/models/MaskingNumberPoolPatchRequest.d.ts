/**
 * 1. All are optional by atleast one is mandatory
 * 2. If appType is EXTERNAL then flowName is Mandatory field.
 *
 */
export declare type MaskingNumberPoolPatchRequest = {
    AppType?: MaskingNumberPoolPatchRequest.AppType;
    UsePin?: boolean;
    AddedNumbers?: Array<string>;
    RemovedNumbers?: Array<string>;
    /**
     * * This field contains flowId for VDT flow or http url or None ( empty ) .
 * * Mandatory if appType is EXTERNAL
 *
     */
    FlowName?: string;
    /**
     * Supported values are GET and POST.
 *
     */
    FlowHttpMethod?: string;
};
export declare namespace MaskingNumberPoolPatchRequest {
    enum AppType {
        EXTERNAL = "EXTERNAL",
        INTERNAL = "INTERNAL"
    }
}
